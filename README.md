This is a [Next.js](https://nextjs.org/) project bootstrapped manually to match create-next-app defaults.

## Getting Started

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
